<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="/task.css">
</head>
<body>
<div class="container task-container">
  <form action="/edit-task/<%= task._id%>" method="POST">
    <label for="title"> Title</label>
    <input type="text" id="title" name="title" placeholder="Title..." value="<%= task.title %>">
    <label for="task">Task</label>
    <textarea id="task" name="task" placeholder="Write your task..." style="height:200px"><%= task.task %></textarea>
    <button class="pill-button" type="submit" value="Submit" >Edit task</button>
  </form>
</div>
</body>
</html>
